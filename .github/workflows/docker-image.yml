name: Publish Docker
on: 
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Publish to Registry
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: registry.cn-beijing.aliyuncs.com/db/$(date +%s)
        username: aaron_8473042
        password: gr123465!
        tag_names: true
        cache: true
